---
export interface Props {
    items: string[];
    style: string;
}

const { items, style = "list-none" } = Astro.props;
---

<ul class:list={["list-inside", style]}>
    {items.map((item: string) => <li set:html={item} />)}
</ul>